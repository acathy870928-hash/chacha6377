# 태깅 결과물 (2026-08-12 기준 문서 목록)

`AI_Datum_List20260812.xlsx`(14,097건)에 `.claude/skills/document-tagging` 규칙을 적용한 결과다.

| 파일 | 내용 |
| --- | --- |
| `AI_Datum_List20260812_태깅결과.xlsx` | 원본 컬럼 + `태깅여부 / 태그1~3 / 판단근거` |
| `태그체계_최종검토리포트.md` | 태그 종류 수, 태그별 문서 수, 저빈도·중복 태그, 통합 권장 |

## 재현 방법

```bash
python3 .claude/skills/document-tagging/scripts/tag_documents.py \
    AI_Datum_List20260812.xlsx \
    -o output/AI_Datum_List20260812_태깅결과.xlsx \
    --json-out /tmp/tagged.json

python3 .claude/skills/document-tagging/scripts/review_tags.py \
    /tmp/tagged.json -o output/태그체계_최종검토리포트.md
```

## 결과 요약

- 태깅 대상 **9,432건** / 제외 **4,665건** (약관 598, 상품요약서 1,732, 사업방법서 1,883, 상품설명서·안내장 64, 도서 388)
- 사용된 태그 **60종**, 문서당 평균 **1.50개**, 태그당 평균 **235건**
- 1개 문서에서만 쓰인 태그: **없음** (최저 `#시책` 3건)

## 통합 권장에 대한 판단

리포트 5번 항목은 기계적으로 뽑은 후보다. 실제 적용 여부는 아래와 같이 판단했다.

| 후보 | 판단 | 이유 |
| --- | --- | --- |
| `#상속` `#증여` `#양도소득세` → `#세금` | **통합하지 않음** | 세무 문서가 783건으로 가장 큰 덩어리다. 여기서 상속·증여·양도를 구분하지 못하면 `#세금` 하나로는 검색이 사실상 불가능해진다. 세 태그 모두 다른 문서에서 반복 사용된다. |
| `#영업화법` → `#교육` | **통합하지 않음** | 화법 자료 중 교육 과정이 아닌 것(스크립트·판매 전략 단독 자료)이 따로 있다. |
| `#주택연금` → `#부동산` | **유지하되 관찰** | 34건 전부 `#부동산`과 함께 붙었다. 주택연금 문서가 더 늘지 않으면 다음 검토 때 `#부동산` + `#노후준비`로 흡수 가능. |
| `#시책` (3건) | **유지** | 저빈도지만 시책·프로모션 자료는 주기적으로 등록되는 성격이라 앞으로 재사용될 태그다. |
| `#보험상식` (545건) | **유지하되 관찰** | 다른 태그를 찾지 못했을 때의 기본값이다. 이 안에서 반복되는 주제가 보이면 새 태그로 승격한다. |

## 다음 문서 묶음을 태깅할 때

같은 스크립트를 그대로 돌리면 된다. 새로 등장한 주제 때문에 `#보험상식`으로 몰리는 문서가 늘어나면
`scripts/tag_rules.json`에 규칙을 추가하고 `references/tag-dictionary.md`에 태그 정의를 함께 적는다.
