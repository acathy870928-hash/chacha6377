# 어드민(사업단 대표) 로그인 비밀번호
ADMIN_PASSWORD=change-me

# 수집기가 기사를 밀어넣을 때 쓰는 API 키
INGEST_API_KEY=change-me-too

# 카톡에 뿌릴 링크의 바깥 주소 (OG 태그/공유 문구에 사용)
PUBLIC_BASE_URL=http://localhost:8000

# SQLite 파일 경로
DB_PATH=./data/news.db
