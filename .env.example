# Anthropic API 키 (없으면 규칙 기반 폴백 모드로 동작합니다)
ANTHROPIC_API_KEY=sk-ant-...

# 사용할 모델 (기본값: claude-opus-5)
CHATBOT_MODEL=claude-opus-5

# 추론 강도: low | medium | high | xhigh | max
# 챗봇은 응답 속도가 중요하므로 low 를 기본값으로 둡니다.
CHATBOT_EFFORT=low

# 서버
HOST=0.0.0.0
PORT=8000
