# Insurance AI — FA 보험 상담 AI

**iFA(아이에프에이) AX팀. 2026년 10월 1일 1차 오픈.**
보험설계사(FA)가 선배 설계사나 지점장에게 묻던 것을 대체하는 상담 AI.

이 파일은 **새 세션이 맥락을 되찾는 자리**다. 먼저 이것부터 읽고,
필요한 문서를 골라 들어가면 된다.

---

## 이 제품이 무엇인가

| | |
| --- | --- |
| 사용자 | **FA(설계사)**. 고객 직접 사용은 2차 |
| 채널 | 데스크톱 웹 챗봇 |
| 소스 | **Micky DATA**(보험 일반지식) + **VELUGA**(보험약관) **2종만** |
| 주 용도 | **보상 질문** — 「이 사고 보험금 나와요?」 |
| 1차 범위 | 약관 스코프 확정 · 면책 우선 답변 · 약관북 · 미등록 약관 확보 요청 |
| 2차 | My Data 연동 · 보장분석 결합 · **금액 답변** |

**신규 서비스다.** 기존 Orca AI(iFA Ai Link)의 확장이 아니다.

---

## 설계의 뿌리 — 이걸 모르면 나머지가 설명되지 않는다

**약관은 서로 80~90%가 똑같은 책들이다.**
100권을 섞어놓고 「질문과 닮은 조각」을 찾게 하면 아무거나 집어온다.
게다가 검색은 구조상 「모르겠다」를 출력할 수 없어 **1등은 반드시 나온다** —
틀려도 틀렸다는 신호가 어디에도 남지 않는다.

그래서 **찾기 전에 약관 한 권을 정확히 짚어낸다.**
사람(베테랑 담당자)이 원래 하던 순서를 그대로 시키는 것이다.

```
1단계  어느 약관인지 짚어낸다      보험사 → 상품 → 약관 시점
2단계  그 약관 한 권만 펼친다      밖의 조항은 점수가 높아도 버린다
3단계  정해진 순서로 확인한다      면책을 건너뛸 수 없게
4단계  근거와 함께 답한다          「제5조 제2항에 따라 …」
```

---

## 절대 어기면 안 되는 규칙

작업 중 판단이 갈리면 이 표로 돌아온다. 전체는 `docs/requirements.md` 2.2절.

| # | 규칙 |
| --- | --- |
| R1 | **약관이 확정되기 전에는 검색하지 않는다** |
| R2 | **어느 약관으로 답하고 있는지 항상 화면에 표시한다** |
| R3 | **스코프 밖 결과는 점수가 높아도 버린다** |
| R4 | 근거가 없으면 **「해당 조항 없음」**으로 끝낸다. 순위를 낮춰 채우지 않는다 |
| R5 | **면책이 먼저다.** 면책에 걸리면 지급사유가 있어도 뒤집지 못한다 |
| R6 | **약관 시점을 추측하지 않는다.** 최신 약관으로 과거 계약에 답하지 않는다 |
| R7 | **약관의 담보 ≠ 가입한 담보.** 약관에는 가입 *가능한* 담보가 전부 있다 |
| R8 | **1차에서 금액을 말하지 않는다.** 가입금액은 2차에 온다 |
| R9 | 면책 · 미가입에 **0원을 쓰지 않는다.** 숫자가 아니라 사유를 둔다 |
| R14 | **고객을 만들지 않아도 물을 수 있다.** 등록을 앞세우면 그 한 번이 사라진다 |
| R15 | **고객 폴더는 그 고객 약관으로만 답한다** |
| R16 | **답할 수 있는 범위를 숫자로 밝힌다** (계약 4건 ≠ 답할 수 있는 4건) |

---

## 용어 — 표기를 흔들지 말 것

- **화면 문구에서는 「판」이 아니라 「약관」**. 「2021.09 시행 약관」 · 「약관 3건」.
  「판」은 **내부 용어**다 — 문서 본문과 코드(`versioning.py` 등)에는 그대로 남아 있고,
  이건 의도된 것이다. FA에게 보이는 곳만 바꾼다
- **보종**(장기 · 자동차 · 일반 · 통합 · 퇴직연금)은 **선택 단계가 아니다.**
  상품의 분류 속성이며 검색 결과 꼬리표로만 쓴다
- **보장맵** — 고객 → 계약 → 담보 3층. 「보장트리」 · 「보장분석」으로 부르지 않는다
- **약관북** — 고객별 약관 폴더. 상담과 나란한 최상위 메뉴
- 화면 문구는 「고객 약관 등록하고 확인하기」가 아니라 **「약관 등록하기」**

---

## 어디에 무엇이 있나

### 문서 (`docs/`)

| 파일 | 내용 |
| --- | --- |
| `requirements.md` | **요구사항 정의서 38건** (1차 33 · 2차 5). 기술 요구사항만. 담당 칸은 비어 있음 |
| `product.md` | 제품 정의 · 답변 두 층 · 면책 우선 |
| `plan.md` | 도입 기획 · 우선순위 · 무엇을 먼저 살 것인가 |
| `data-architecture.md` | 데이터 소스 7종 · **벤더 확인 항목** |
| `vendor-questions.md` | **벤더 확인 질의서 19건.** 그대로 보낼 수 있는 형태. 답에 따라 기획이 어디로 갈리는지 함께 |
| `pipeline.md` | 약관 확보 → 색인 → 검수 → 알림 |
| `rollout.md` · `cost.md` | 등록 전략 · **파일 단위 과금** 계산 |
| `privacy.md` | 개인정보 분리 설계 (벤더에 무엇을 안 보내는가) |
| `clarification-flow.md` · `routing.md` | 되묻기 · 라우팅 |
| `market.md` · `coverage-analysis.md` · `seed-products.md` | 시장 · 보장분석 · 시드 상품 |

### 화면 (`docs/mockups/`)

| 파일 | 내용 |
| --- | --- |
| `insurance-ai.html` | **상담설계** — 흐름 A~J · 설계 규칙 19개 · 화면↔코드 매핑 |
| `insurance-ai-demo.html` | **작동하는 챗봇 프로토타입.** 실제로 눌러 볼 수 있음 |

### 코드 (`src/orca/`) — 규칙을 코드로 고정한 곳

| 모듈 | 하는 일 |
| --- | --- |
| `scope.py` | **TermsScope · require_locked() · confine() · CustomerScope**. R1~R4가 여기 있다 |
| `payout.py` | **judge()** — 면책 우선 판정. R5 |
| `routing.py` · `types.py` | 질문 분류(LLM) → 라우팅 판단(규칙 코드) |
| `versioning.py` | 계약년월 → 시행 중이던 약관 |
| `clarify.py` | 되묻기 turn planner (13 NextAction) |
| `coverage.py` · `coverage_map.py` | 보장맵. 기본값 **「확인 필요」** |
| `links.py` | 약관북 (고객 ↔ 약관 장부) |
| `customers.py` · `registration.py` | 고객 등록(이름 하나 + 복수 메모) · 등록 경로 |
| `requests.py` · `pricing.py` | 미등록 약관 확보 요청 · 파일 단위 과금 |
| `vendor_query.py` | **벤더에 PII를 안 보내는 것을 코드로 강제** |

**테스트 343개.** `python -m pytest -q`

### 생성 도구 (`tools/`)

```
python3 tools/md2html.py    # requirements.md → docs/requirements.html
python3 tools/md2pdf.py     # requirements.md → 정의서 PDF (A4 세로)
python3 tools/shots.py      # 목업 화면 → PNG (슬라이드용)
node tools/deck.js          # PPT 생성 (pptxgenjs)
node tools/demo_smoke.js    # 프로토타입 회귀 테스트 6항목
```

문서를 고칠 때는 **`requirements.md`만 고치면** HTML · PDF가 함께 나온다.

---

## 지금 상태

- **정의서 · 화면 · 코드 세 벌이 같은 말을 한다.** 표기와 규칙이 맞춰져 있음
- 프로토타입은 손보 14 · 생보 16 보험사, 상품 22개, 조항 31건으로 돈다

### 막혀 있는 것 — 벤더에 확인해야 계획이 선다

**질의서는 `docs/vendor-questions.md`에 19건으로 정리돼 있다. 그대로 보내면 된다.**

1. **판매 종료 상품 색인 가능 여부** — 불가하면 보상용 약관을 살 수 없다. **기획 전제가 무너진다**
2. **상품 · 약관 시점 메타데이터 필터 지원** — 없으면 스코프를 확정해도 벤더가 100권을 뒤진다
3. **약관 파일 단가와 과금 단위** — 미확정이라 화면에 금액을 못 띄운다
4. 상품당 파일 배수 · 근거 청크 반환 · 유사도 점수 반환

내부 확정 필요: 1차 오픈 종목 · FA 규모 · 알림 채널 · 초기 200개 구성
법무: 보장분석 · My Data 동의 범위 (2차 선행 조건, 1차는 막지 않음)

---

## 작업 규칙

- 브랜치 **`claude/value-chatbot-dev-ijju99`**. 여기서만 작업하고 푸시한다
- 커밋 메시지는 **무엇을 왜 바꿨는지** 한국어로. 규칙을 건드렸으면 그 이유를 적는다
- 문서 · 화면 · 코드 중 하나만 고치면 어긋난다. **셋을 함께 본다**
- 사용자는 기획자다. 코드보다 **화면과 문서로 확인**하는 것을 선호한다
